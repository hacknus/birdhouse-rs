services:
  birdhouse:
    build: .
    container_name: birdhouse-rs
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./public/gallery_cache:/usr/local/app/public/gallery_cache
      - ./data:/usr/local/app/data
    restart: unless-stopped